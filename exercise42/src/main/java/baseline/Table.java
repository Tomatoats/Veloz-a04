package baseline;

import java.util.ArrayList;

public class Table {
    public Table(ArrayList<String> input) {
        //aight so this is gonna be kinda easy
        //Make a function to output the input

        TableOutput(input);
    }

    private void TableOutput(ArrayList<String> input) {
        //set up printstream stuff

        //Print out the initial table, yknow "last first Salary " and all the ---

        //in a for loop from 0 to the size of the array:
        //if i is equal to three, then have a %n before the string
        //else just print the string
    }
}
